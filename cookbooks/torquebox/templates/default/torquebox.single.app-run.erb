#!/bin/bash
# File managed by Chef-Solo

BIND_ADDR=<%= node[:torquebox][:bind_addr] %>
BIND_PORT=<%= node[:torquebox][:bind_port] %>
APP_DIR=<%= node[:torquebox][:app_dir] %>
LOG_DIR=<%= node[:torquebox][:log_dir] %>

#echo "Executing: 'torquebox run -b ${BIND_ADDR} -p ${BIND_PORT}' on $APP_DIR"
torquebox run -b ${BIND_ADDR} -p ${BIND_PORT} > ${LOG_DIR}/lastrun.log 2>&1 &
#echo "Log information can be found at ${LOG_DIR}/lastrun.log"
 


